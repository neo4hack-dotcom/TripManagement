# APP_URL: The URL where this applet is hosted.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# VITE_API_BASE_URL: Frontend base URL for the Python backend API.
# In local dev, leave empty to use the Vite proxy to FastAPI.
VITE_API_BASE_URL=""
